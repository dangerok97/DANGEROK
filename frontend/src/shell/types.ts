/**
 * ORA Application Shell V1 — presentation modes.
 * Ambient = Home/tabs life OS; Focus = one-task chrome; Immersive = full attention (Life Setup / deep conv).
 */
export type OraShellMode = 'ambient' | 'focus' | 'immersive';
